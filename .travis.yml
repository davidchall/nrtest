language: python

# python versions to test
python:
 - "2.7"
 - "3.2"
 - "3.3"
 - "3.4"

# install prerequisites and dependencies
install:
  - pip install pep8 coveralls
  - pip install git+https://github.com/davidchall/progression.git
  - pip install -e .

# run PEP-8 compliance test and unit tests
script:
  - pep8 progression testing
  - nosetests testing --with-coverage --cover-package=progression

# Submit data to coveralls
after_success:
  coveralls
