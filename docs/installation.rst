.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install nrtest

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv nrtest
    $ pip install nrtest
